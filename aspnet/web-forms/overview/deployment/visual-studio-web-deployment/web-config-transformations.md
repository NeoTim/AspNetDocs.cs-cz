---
uid: web-forms/overview/deployment/visual-studio-web-deployment/web-config-transformations
title: 'ASP.NET nasazení webu pomocí sady Visual Studio: Transformace souborů Web.config | Dokumenty společnosti Microsoft'
author: tdykstra
description: Tato série kurzů ukazuje, jak nasadit (publikovat) ASP.NET webovou aplikaci do webových aplikací Služby App Service Azure nebo poskytovateli hostingu třetí strany, usin...
ms.author: riande
ms.date: 02/15/2013
ms.assetid: 5a2a927b-14cb-40bc-867a-f0680f9febd7
msc.legacyurl: /web-forms/overview/deployment/visual-studio-web-deployment/web-config-transformations
msc.type: authoredcontent
ms.openlocfilehash: a9d39547c94a63003442ba6fe1257693dde24b05
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/06/2020
ms.locfileid: "80676123"
---
# <a name="aspnet-web-deployment-using-visual-studio-webconfig-file-transformations"></a><span data-ttu-id="95eaa-103">ASP.NET nasazení webu pomocí sady Visual Studio: Transformace souborů Web.config</span><span class="sxs-lookup"><span data-stu-id="95eaa-103">ASP.NET Web Deployment using Visual Studio: Web.config File Transformations</span></span>

<span data-ttu-id="95eaa-104">podle [Tom Dykstra](https://github.com/tdykstra)</span><span class="sxs-lookup"><span data-stu-id="95eaa-104">by [Tom Dykstra](https://github.com/tdykstra)</span></span>

[<span data-ttu-id="95eaa-105">Stáhnout počáteční projekt</span><span class="sxs-lookup"><span data-stu-id="95eaa-105">Download Starter Project</span></span>](https://go.microsoft.com/fwlink/p/?LinkId=282627)

> <span data-ttu-id="95eaa-106">Tato série kurzů ukazuje, jak nasadit (publikovat) ASP.NET webové aplikace do Webových aplikací služby Azure App Service nebo poskytovateli hostingu třetí strany pomocí Visual Studia 2012 nebo Visual Studia 2010.</span><span class="sxs-lookup"><span data-stu-id="95eaa-106">This tutorial series shows you how to deploy (publish) an ASP.NET web application to Azure App Service Web Apps or to a third-party hosting provider, by using Visual Studio 2012 or Visual Studio 2010.</span></span> <span data-ttu-id="95eaa-107">Informace o sérii naleznete [v prvním kurzu v sérii](introduction.md).</span><span class="sxs-lookup"><span data-stu-id="95eaa-107">For information about the series, see [the first tutorial in the series](introduction.md).</span></span>

## <a name="overview"></a><span data-ttu-id="95eaa-108">Přehled</span><span class="sxs-lookup"><span data-stu-id="95eaa-108">Overview</span></span>

<span data-ttu-id="95eaa-109">Tento kurz ukazuje, jak automatizovat proces změny souboru *Web.config* při jeho nasazení do různých cílových prostředí.</span><span class="sxs-lookup"><span data-stu-id="95eaa-109">This tutorial shows you how to automate the process of changing the *Web.config* file when you deploy it to different destination environments.</span></span> <span data-ttu-id="95eaa-110">Většina aplikací má v souboru *Web.config* nastavení, která se při nasazení aplikace musí lišit.</span><span class="sxs-lookup"><span data-stu-id="95eaa-110">Most applications have settings in the *Web.config* file that must be different when the application is deployed.</span></span> <span data-ttu-id="95eaa-111">Automatizace procesu provádění těchto změn zabrání nutnosti provádět je ručně při každém nasazení, což by bylo únavné a náchylné k chybám.</span><span class="sxs-lookup"><span data-stu-id="95eaa-111">Automating the process of making these changes keeps you from having to do them manually every time you deploy, which would be tedious and error prone.</span></span>

<span data-ttu-id="95eaa-112">Připomenutí: Pokud se vám zobrazí chybová zpráva nebo něco nefunguje, když procházíte kurzem, nezapomeňte se podívat na [stránku pro řešení potíží](troubleshooting.md).</span><span class="sxs-lookup"><span data-stu-id="95eaa-112">Reminder: If you get an error message or something doesn't work as you go through the tutorial, be sure to check the [troubleshooting page](troubleshooting.md).</span></span>

## <a name="webconfig-transformations-versus-web-deploy-parameters"></a><span data-ttu-id="95eaa-113">Transformace web.config versus parametry nasazení webu</span><span class="sxs-lookup"><span data-stu-id="95eaa-113">Web.config transformations versus Web Deploy parameters</span></span>

<span data-ttu-id="95eaa-114">Proces změny nastavení souboru *Web.config* lze automatizovat dvěma [způsoby: transformace web.config](https://msdn.microsoft.com/library/dd465326.aspx) a [parametry nasazení webu](https://msdn.microsoft.com/library/ff398068.aspx).</span><span class="sxs-lookup"><span data-stu-id="95eaa-114">There are two ways to automate the process of changing *Web.config* file settings: [Web.config transformations](https://msdn.microsoft.com/library/dd465326.aspx) and [Web Deploy parameters](https://msdn.microsoft.com/library/ff398068.aspx).</span></span> <span data-ttu-id="95eaa-115">Transformační soubor *Web.config* obsahuje značky XML, které určují, jak změnit soubor *Web.config* při jeho nasazení.</span><span class="sxs-lookup"><span data-stu-id="95eaa-115">A *Web.config* transformation file contains XML markup that specifies how to change the *Web.config* file when it is deployed.</span></span> <span data-ttu-id="95eaa-116">Můžete zadat různé změny pro konkrétní konfigurace sestavení a pro konkrétní profily publikování.</span><span class="sxs-lookup"><span data-stu-id="95eaa-116">You can specify different changes for specific build configurations and for specific publish profiles.</span></span> <span data-ttu-id="95eaa-117">Výchozí konfigurace sestavení jsou Ladění a vydání a můžete vytvořit vlastní konfigurace sestavení.</span><span class="sxs-lookup"><span data-stu-id="95eaa-117">The default build configurations are Debug and Release, and you can create custom build configurations.</span></span> <span data-ttu-id="95eaa-118">Profil publikování obvykle odpovídá cílovému prostředí.</span><span class="sxs-lookup"><span data-stu-id="95eaa-118">A publish profile typically corresponds to a destination environment.</span></span> <span data-ttu-id="95eaa-119">(Další informace o publikování profilů najdete v kurzu [Nasazení do služby IIS jako testovacího prostředí.)](deploying-to-iis.md)</span><span class="sxs-lookup"><span data-stu-id="95eaa-119">(You'll learn more about publish profiles in the [Deploying to IIS as a Test Environment](deploying-to-iis.md) tutorial.)</span></span>

<span data-ttu-id="95eaa-120">Parametry nasazení webu lze použít k určení mnoha různých druhů nastavení, které musí být nakonfigurovány během nasazení, včetně nastavení, která se nacházejí v souborech *Web.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-120">Web Deploy parameters can be used to specify many different kinds of settings that must be configured during deployment, including settings that are found in *Web.config* files.</span></span> <span data-ttu-id="95eaa-121">Při použití k určení změn souborů *Web.config* jsou parametry nasazení webu složitější, ale jsou užitečné, pokud neznáte hodnotu, která má být nastavena, dokud nenasadíte.</span><span class="sxs-lookup"><span data-stu-id="95eaa-121">When used to specify *Web.config* file changes, Web Deploy parameters are more complex to set up, but they are useful when you do not know the value to be set until you deploy.</span></span> <span data-ttu-id="95eaa-122">Například v podnikovém prostředí můžete vytvořit *balíček nasazení* a předat jej osobě v oddělení IT k instalaci v produkčním prostředí a tato osoba musí být schopna zadat připojovací řetězce nebo hesla, která neznáte.</span><span class="sxs-lookup"><span data-stu-id="95eaa-122">For example, in an enterprise environment, you might create a *deployment package* and give it to a person in the IT department to install in production, and that person has to be able to enter connection strings or passwords that you do not know.</span></span>

<span data-ttu-id="95eaa-123">Pro scénář, který tato série kurzů pokrývá, víte předem vše, co je třeba udělat pro soubor *Web.config,* takže není nutné používat parametry nasazení webu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-123">For the scenario that this tutorial series covers, you know in advance everything that has to be done to the *Web.config* file, so you do not need to use Web Deploy parameters.</span></span> <span data-ttu-id="95eaa-124">Nakonfigurujete některé transformace, které se liší v závislosti na použité konfiguraci sestavení a některé, které se liší v závislosti na použitém profilu publikování.</span><span class="sxs-lookup"><span data-stu-id="95eaa-124">You'll configure some transformations that differ depending on the build configuration used, and some that differ depending on the publish profile used.</span></span>

<a id="watransforms"></a>

## <a name="specifying-webconfig-settings-in-azure"></a><span data-ttu-id="95eaa-125">Určení nastavení web.config v Azure</span><span class="sxs-lookup"><span data-stu-id="95eaa-125">Specifying Web.config settings in Azure</span></span>

<span data-ttu-id="95eaa-126">Pokud nastavení souboru *Web.config,* které chcete `<connectionStrings>` změnit, jsou v `<appSettings>` prvku nebo a pokud nasazujete do webových aplikací ve službě Azure App Service, máte další možnost automatizace změn během nasazení.</span><span class="sxs-lookup"><span data-stu-id="95eaa-126">If the *Web.config* file settings that you want to change are in the `<connectionStrings>` or the `<appSettings>` element, and if you are deploying to Web Apps in Azure App Service, you have another option for automating changes during deployment.</span></span> <span data-ttu-id="95eaa-127">Nastavení, která se mají projevit v Azure, můžete zadat na kartě **Konfigurace** na stránce portálu pro správu webové aplikace (přejděte dolů k **části nastavení aplikace** a **připojovacích řetězců).**</span><span class="sxs-lookup"><span data-stu-id="95eaa-127">You can enter the settings that you want to take effect in Azure in the **Configure** tab of the management portal page for your web app (scroll down to the **app settings** and **connection strings** sections).</span></span> <span data-ttu-id="95eaa-128">Když nasadíte projekt, Azure automaticky použije změny.</span><span class="sxs-lookup"><span data-stu-id="95eaa-128">When you deploy the project, Azure automatically applies the changes.</span></span> <span data-ttu-id="95eaa-129">Další informace naleznete v [tématu Weby Windows Azure: Jak fungují aplikační řetězce a připojovací řetězce](https://blogs.msdn.com/b/windowsazure/archive/2013/07/17/windows-azure-web-sites-how-application-strings-and-connection-strings-work.aspx).</span><span class="sxs-lookup"><span data-stu-id="95eaa-129">For more information, see [Windows Azure Web Sites: How Application Strings and Connection Strings Work](https://blogs.msdn.com/b/windowsazure/archive/2013/07/17/windows-azure-web-sites-how-application-strings-and-connection-strings-work.aspx).</span></span>

## <a name="default-transformation-files"></a><span data-ttu-id="95eaa-130">Výchozí transformační soubory</span><span class="sxs-lookup"><span data-stu-id="95eaa-130">Default transformation files</span></span>

<span data-ttu-id="95eaa-131">V **Průzkumníku řešení**rozbalte *web.config,* abyste viděli transformační soubory *Web.Debug.config* a *Web.Release.config,* které jsou ve výchozím nastavení vytvořeny pro dvě výchozí konfigurace sestavení.</span><span class="sxs-lookup"><span data-stu-id="95eaa-131">In **Solution Explorer**, expand *Web.config* to see the *Web.Debug.config* and *Web.Release.config* transformation files that are created by default for the two default build configurations.</span></span>

![Web.config_transform_files](web-config-transformations/_static/image1.png)

<span data-ttu-id="95eaa-133">Transformační soubory pro vlastní konfigurace sestavení můžete vytvořit tak, že klepnete pravým tlačítkem myši na soubor Web.config a z kontextové nabídky zvolíte **Přidat transformace konfigurace.**</span><span class="sxs-lookup"><span data-stu-id="95eaa-133">You can create transformation files for custom build configurations by right-clicking the Web.config file and choosing **Add Config Transforms** from the context menu.</span></span> <span data-ttu-id="95eaa-134">Pro účely tohoto kurzu to nemusíte dělat a možnost nabídky je zakázána, protože jste nevytvořili žádné vlastní konfigurace sestavení.</span><span class="sxs-lookup"><span data-stu-id="95eaa-134">For this tutorial you don't need to do that, and the menu option is disabled, because you haven't created any custom build configurations.</span></span>

<span data-ttu-id="95eaa-135">Později vytvoříte další tři transformační soubory, po jednom pro profily publikování testu, pracovní a produkční.</span><span class="sxs-lookup"><span data-stu-id="95eaa-135">Later you'll create three more transformation files, one each for the test, staging, and production publish profiles.</span></span> <span data-ttu-id="95eaa-136">Typickým příkladem nastavení, které byste zpracovat v souboru transformace profilu publikování, protože závisí na cílovém prostředí je koncový bod WCF, který se liší pro test versus výroba.</span><span class="sxs-lookup"><span data-stu-id="95eaa-136">A typical example of a setting that you would handle in a publish profile transformation file because it depends on the destination environment is a WCF endpoint that is different for test versus production.</span></span> <span data-ttu-id="95eaa-137">Po vytvoření profilů publikování, které jsou s ním, vytvoříte soubory transformace profilu publikování v pozdějších kurzech.</span><span class="sxs-lookup"><span data-stu-id="95eaa-137">You'll create publish profile transformation files in later tutorials after you create the publish profiles that they go with.</span></span>

## <a name="disable-debug-mode"></a><span data-ttu-id="95eaa-138">Zakázání režimu ladění</span><span class="sxs-lookup"><span data-stu-id="95eaa-138">Disable debug mode</span></span>

<span data-ttu-id="95eaa-139">Příkladem nastavení, které závisí na konfiguraci sestavení, `debug` nikoli na cílovém prostředí, je atribut.</span><span class="sxs-lookup"><span data-stu-id="95eaa-139">An example of a setting that depends on build configuration rather than destination environment is the `debug` attribute.</span></span> <span data-ttu-id="95eaa-140">Pro sestavení verze obvykle chcete ladění zakázáno bez ohledu na to, do kterého prostředí nasazujete.</span><span class="sxs-lookup"><span data-stu-id="95eaa-140">For a Release build, you typically want debugging disabled regardless of which environment you are deploying to.</span></span> <span data-ttu-id="95eaa-141">Proto ve výchozím nastavení šablony projektu sady Visual Studio vytvořit *Web.Release.config transformovat* soubory s kódem, `debug` `compilation` který odebere atribut z prvku.</span><span class="sxs-lookup"><span data-stu-id="95eaa-141">Therefore, by default the Visual Studio project templates create *Web.Release.config* transform files with code that removes the `debug` attribute from the `compilation` element.</span></span> <span data-ttu-id="95eaa-142">Zde je výchozí *Web.Release.config*: kromě některé ukázkové transformační kód, který `compilation` je zakomentován, obsahuje kód v elementu, který odebere `debug` atribut:</span><span class="sxs-lookup"><span data-stu-id="95eaa-142">Here is the default *Web.Release.config*: in addition to some sample transformation code that is commented out, it includes code in the `compilation` element that removes the `debug` attribute:</span></span>

[!code-xml[Main](web-config-transformations/samples/sample1.xml?highlight=18)]

<span data-ttu-id="95eaa-143">Atribut `xdt:Transform="RemoveAttributes(debug)"` určuje, že chcete `debug` atribut odebrat `system.web/compilation` z prvku v nasazeném souboru *Web.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-143">The `xdt:Transform="RemoveAttributes(debug)"` attribute specifies that you want the `debug` attribute to be removed from the `system.web/compilation` element in the deployed *Web.config* file.</span></span> <span data-ttu-id="95eaa-144">To se provede při každém nasazení sestavení verze.</span><span class="sxs-lookup"><span data-stu-id="95eaa-144">This will be done every time you deploy a Release build.</span></span>

## <a name="limit-error-log-access-to-administrators"></a><span data-ttu-id="95eaa-145">Omezit přístup k protokolu chyb správcům</span><span class="sxs-lookup"><span data-stu-id="95eaa-145">Limit error log access to administrators</span></span>

<span data-ttu-id="95eaa-146">Pokud dojde k chybě při spuštění aplikace, aplikace zobrazí obecnou chybovou stránku namísto systémově generované chybové stránky a používá [balíček Elmah NuGet](http://www.hanselman.com/blog/NuGetPackageOfTheWeek7ELMAHErrorLoggingModulesAndHandlersWithSQLServerCompact.aspx) pro protokolování chyb a vytváření sestav.</span><span class="sxs-lookup"><span data-stu-id="95eaa-146">If there's an error while the application runs, the application displays a generic error page in place of the system-generated error page, and it uses the [Elmah NuGet package](http://www.hanselman.com/blog/NuGetPackageOfTheWeek7ELMAHErrorLoggingModulesAndHandlersWithSQLServerCompact.aspx) for error logging and reporting.</span></span> <span data-ttu-id="95eaa-147">Prvek `customErrors` v souboru *Web.config* aplikace určuje chybovou stránku:</span><span class="sxs-lookup"><span data-stu-id="95eaa-147">The `customErrors` element in the application *Web.config* file specifies the error page:</span></span>

[!code-xml[Main](web-config-transformations/samples/sample2.xml)]

<span data-ttu-id="95eaa-148">Chcete-li zobrazit chybovou stránku, dočasně změňte `mode` atribut `customErrors` prvku z "RemoteOnly" na "On" a spusťte aplikaci z aplikace Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="95eaa-148">To see the error page, temporarily change the `mode` attribute of the `customErrors` element from "RemoteOnly" to "On" and run the application from Visual Studio.</span></span> <span data-ttu-id="95eaa-149">Způsobit chybu vyžádáním neplatné adresy URL, například *Studentsxxx.aspx*.</span><span class="sxs-lookup"><span data-stu-id="95eaa-149">Cause an error by requesting an invalid URL, such as *Studentsxxx.aspx*.</span></span> <span data-ttu-id="95eaa-150">Místo chybové stránky "Prostředek nebyl nalezen" vygenerovaná ze strany IIS se zobrazí stránka *GenericErrorPage.aspx.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-150">Instead of an IIS-generated "The resource cannot be found" error page, you see the *GenericErrorPage.aspx* page.</span></span>

![Chybová stránka](web-config-transformations/_static/image2.png)

<span data-ttu-id="95eaa-152">Chcete-li zobrazit protokol chyb, nahraďte vše v adrese URL za číslem portu *elmah.axd* `http://localhost:51130/elmah.axd`(například) a stiskněte Enter:</span><span class="sxs-lookup"><span data-stu-id="95eaa-152">To see the error log, replace everything in the URL after the port number with *elmah.axd* (for example, `http://localhost:51130/elmah.axd`) and press Enter:</span></span>

![Stránka ELMAH](web-config-transformations/_static/image3.png)

<span data-ttu-id="95eaa-154">Nezapomeňte nastavit `customErrors` prvek zpět do režimu "RemoteOnly", když budete hotovi.</span><span class="sxs-lookup"><span data-stu-id="95eaa-154">Don't forget to set the `customErrors` element back to "RemoteOnly" mode when you're done.</span></span>

<span data-ttu-id="95eaa-155">Ve vývojovém počítači je vhodné povolit volný přístup ke stránce protokolu chyb, ale v produkčním prostředí by to bylo bezpečnostní riziko.</span><span class="sxs-lookup"><span data-stu-id="95eaa-155">On your development computer it's convenient to allow free access to the error log page, but in production that would be a security risk.</span></span> <span data-ttu-id="95eaa-156">Pro produkční lokalitu chcete přidat autorizační pravidlo, které omezuje přístup k protokolu chyb správcům, a ujistěte se, že omezení funguje, chcete-li v testu a přípravě také.</span><span class="sxs-lookup"><span data-stu-id="95eaa-156">For the production site, you want to add an authorization rule that restricts error log access to administrators, and to make sure that the restriction works you want it in test and staging also.</span></span> <span data-ttu-id="95eaa-157">Proto je to další změna, kterou chcete implementovat při každém nasazení sestavení release, a proto patří do souboru *Web.Release.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-157">Therefore this is another change that you want to implement every time you deploy a Release build, and so it belongs in the *Web.Release.config* file.</span></span>

<span data-ttu-id="95eaa-158">Otevřete *web.Release.config* a `location` přidejte nový `configuration` prvek bezprostředně před uzavírací značku, jak je znázorněno zde.</span><span class="sxs-lookup"><span data-stu-id="95eaa-158">Open *Web.Release.config* and add a new `location` element immediately before the closing `configuration` tag, as shown here.</span></span>

[!code-xml[Main](web-config-transformations/samples/sample3.xml?highlight=27-34)]

<span data-ttu-id="95eaa-159">Hodnota `Transform` atributu "Vložit" `location` způsobí, že tento prvek má `location` být přidán jako na stejné úrovni k existujícím prvkům v souboru *Web.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-159">The `Transform` attribute value of "Insert" causes this `location` element to be added as a sibling to any existing `location` elements in the *Web.config* file.</span></span> <span data-ttu-id="95eaa-160">(Již existuje `location` jeden prvek, který určuje autorizační pravidla pro stránku **Aktualizovat kredity.)**</span><span class="sxs-lookup"><span data-stu-id="95eaa-160">(There is already one `location` element that specifies authorization rules for the **Update Credits** page.)</span></span>

<span data-ttu-id="95eaa-161">Nyní můžete zobrazit náhled transformace a ujistěte se, že jste ji kódovali správně.</span><span class="sxs-lookup"><span data-stu-id="95eaa-161">Now you can preview the transform to make sure that you coded it correctly.</span></span>

<span data-ttu-id="95eaa-162">V **Průzkumníku řešení**klepněte pravým tlačítkem myši na *web.release.config* a klepněte na příkaz **Náhled transformace**.</span><span class="sxs-lookup"><span data-stu-id="95eaa-162">In **Solution Explorer**, right-click *Web.Release.config* and click **Preview Transform**.</span></span>

![Nabídka Transformace náhledu](web-config-transformations/_static/image4.png)

<span data-ttu-id="95eaa-164">Otevře se stránka, která zobrazuje vývojový soubor *Web.config* vlevo a jak bude nasazený soubor *Web.config* vypadat vpravo se zvýrazněnými změnami.</span><span class="sxs-lookup"><span data-stu-id="95eaa-164">A page opens that shows you the development *Web.config* file on the left and what the deployed *Web.config* file will look like on the right, with changes highlighted.</span></span>

![Náhled ladicí transformace](web-config-transformations/_static/image5.png)

![Náhled transformace umístění](web-config-transformations/_static/image6.png)

<span data-ttu-id="95eaa-167">(V náhledu si můžete všimnout některých dalších změn, pro které jste nenapsali transformace: obvykle zahrnují odstranění prázdného místa, které nemá vliv na funkčnost.)</span><span class="sxs-lookup"><span data-stu-id="95eaa-167">( In the preview, you might notice some additional changes that you didn't write transforms for: these typically involve the removal of white space that doesn't affect functionality.)</span></span>

<span data-ttu-id="95eaa-168">Když testujete lokalitu po nasazení, budete také testovat ověřit, zda je pravidlo autorizace účinné.</span><span class="sxs-lookup"><span data-stu-id="95eaa-168">When you test the site after deployment, you'll also test to verify that the authorization rule is effective.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="95eaa-169">**Poznámka k zabezpečení** Nikdy nezobrazovat podrobnosti o chybě veřejnosti v produkční aplikaci ani tyto informace neukládat ve veřejném umístění.</span><span class="sxs-lookup"><span data-stu-id="95eaa-169">**Security Note** Never display error details to the public in a production application, or store that information in a public location.</span></span> <span data-ttu-id="95eaa-170">Útočníci mohou pomocí informací o chybách zjistit chyby zabezpečení na webu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-170">Attackers can use error information to discover vulnerabilities in a site.</span></span> <span data-ttu-id="95eaa-171">Pokud elmah používáte ve vlastní aplikaci, nakonfigurujte ELMAH tak, aby minimalizoval bezpečnostní rizika.</span><span class="sxs-lookup"><span data-stu-id="95eaa-171">If you use ELMAH in your own application, configure ELMAH to minimize security risks.</span></span> <span data-ttu-id="95eaa-172">Elmah příklad v tomto kurzu by neměly být považovány za doporučenou konfiguraci.</span><span class="sxs-lookup"><span data-stu-id="95eaa-172">The ELMAH example in this tutorial should not be considered a recommended configuration.</span></span> <span data-ttu-id="95eaa-173">Jedná se o příklad, který byl vybrán, aby ilustroval, jak zpracovat složku, ve které musí být aplikace schopna vytvářet soubory.</span><span class="sxs-lookup"><span data-stu-id="95eaa-173">It is an example that was chosen in order to illustrate how to handle a folder that the application must be able to create files in.</span></span> <span data-ttu-id="95eaa-174">Další informace naleznete [v tématu zabezpečení koncového bodu ELMAH](https://code.google.com/p/elmah/wiki/SecuringErrorLogPages).</span><span class="sxs-lookup"><span data-stu-id="95eaa-174">For more information, see [securing the ELMAH endpoint](https://code.google.com/p/elmah/wiki/SecuringErrorLogPages).</span></span>

## <a name="a-setting-that-youll-handle-in-publish-profile-transformation-files"></a><span data-ttu-id="95eaa-175">Nastavení, které budete zpracovávat v souborech transformace profilu publikování</span><span class="sxs-lookup"><span data-stu-id="95eaa-175">A setting that you'll handle in publish profile transformation files</span></span>

<span data-ttu-id="95eaa-176">Běžným scénářem je mít nastavení souboru *Web.config,* které se musí lišit v každém prostředí, do kterého nasadíte.</span><span class="sxs-lookup"><span data-stu-id="95eaa-176">A common scenario is to have *Web.config* file settings that must be different in each environment that you deploy to.</span></span> <span data-ttu-id="95eaa-177">Například aplikace, která volá službu WCF může potřebovat jiný koncový bod v testovacím a provozním prostředí.</span><span class="sxs-lookup"><span data-stu-id="95eaa-177">For example, an application that calls a WCF service might need a different endpoint in test and production environments.</span></span> <span data-ttu-id="95eaa-178">Aplikace Contoso University obsahuje také nastavení tohoto druhu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-178">The Contoso University application includes a setting of this kind also.</span></span> <span data-ttu-id="95eaa-179">Toto nastavení řídí viditelný indikátor na stránkách webu, který vám řekne, ve kterém prostředí se právě nacházejíte, například vývoj, testování nebo výrobu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-179">This setting controls a visible indicator on a site's pages that tells you which environment you are in, such as development, test, or production.</span></span> <span data-ttu-id="95eaa-180">Hodnota nastavení určuje, zda aplikace připojí "(Dev)" nebo "(Test)" k hlavnímu nadpisu na stránce předlohy *Site.Master:*</span><span class="sxs-lookup"><span data-stu-id="95eaa-180">The setting value determines whether the application will append "(Dev)" or "(Test)" to the main heading in the *Site.Master* master page:</span></span>

![Ukazatel prostředí](web-config-transformations/_static/image7.png)

<span data-ttu-id="95eaa-182">Indikátor prostředí je vynechán, když je aplikace spuštěna v pracovní nebo produkční.</span><span class="sxs-lookup"><span data-stu-id="95eaa-182">The environment indicator is omitted when the application is running in staging or production.</span></span>

<span data-ttu-id="95eaa-183">Webové stránky Univerzity Contoso čtou `appSettings` hodnotu nastavenou v souboru *Web.config,* aby bylo možné určit, v jakém prostředí je aplikace spuštěna:</span><span class="sxs-lookup"><span data-stu-id="95eaa-183">The Contoso University web pages read a value that is set in `appSettings` in the *Web.config* file in order to determine what environment the application is running in:</span></span>

[!code-xml[Main](web-config-transformations/samples/sample4.xml)]

<span data-ttu-id="95eaa-184">Hodnota by měla být "Test" v testovacím prostředí a "Prod" pro pracovní a produkční.</span><span class="sxs-lookup"><span data-stu-id="95eaa-184">The value should be "Test" in the test environment, and "Prod" for staging and production.</span></span>

<span data-ttu-id="95eaa-185">Následující kód v transformačním souboru implementuje tuto transformaci:</span><span class="sxs-lookup"><span data-stu-id="95eaa-185">The following code in a transform file will implement this transformation:</span></span>

[!code-xml[Main](web-config-transformations/samples/sample5.xml)]

<span data-ttu-id="95eaa-186">Hodnota `xdt:Transform` atributu "SetAttributes" označuje, že účelem této transformace je změna hodnot atributů existujícího prvku v souboru *Web.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-186">The `xdt:Transform` attribute value "SetAttributes" indicates that the purpose of this transform is to change attribute values of an existing element in the *Web.config* file.</span></span> <span data-ttu-id="95eaa-187">Hodnota `xdt:Locator` atributu "Match(key)" označuje, že prvek, `key` který má `key` být změněn, je ten, jehož atribut odpovídá zde zadanému atributu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-187">The `xdt:Locator` attribute value "Match(key)" indicates that the element to be modified is the one whose `key` attribute matches the `key` attribute specified here.</span></span> <span data-ttu-id="95eaa-188">Jediným dalším atributem `add` prvku `value`je a to je to, co se změní v nasazeném souboru *Web.config.*</span><span class="sxs-lookup"><span data-stu-id="95eaa-188">The only other attribute of the `add` element is `value`, and that is what will be changed in the deployed *Web.config* file.</span></span> <span data-ttu-id="95eaa-189">Zde zobrazený kód `value` způsobí, `Environment` `appSettings` že atribut prvku bude nastaven na "Test" v souboru *Web.config,* který je nasazen.</span><span class="sxs-lookup"><span data-stu-id="95eaa-189">The code shown here causes the `value` attribute of the `Environment` `appSettings` element to be set to "Test" in the *Web.config* file that is deployed.</span></span>

<span data-ttu-id="95eaa-190">Tato transformace patří do souborů transformace profilu publikování, které jste ještě nevytvořili.</span><span class="sxs-lookup"><span data-stu-id="95eaa-190">This transform belongs in the publish profile transform files, which you haven't created yet.</span></span> <span data-ttu-id="95eaa-191">Soubory transformace, které tuto změnu implementují, vytvoříte a aktualizujete při vytváření profilů publikování pro testovací, pracovní a produkční prostředí.</span><span class="sxs-lookup"><span data-stu-id="95eaa-191">You'll create and update the transform files that implement this change when you create the publish profiles for the test, staging, and production environments.</span></span> <span data-ttu-id="95eaa-192">To uděláte v [nasazení do služby IIS](deploying-to-iis.md) a [nasazení do produkčních](deploying-to-production.md) kurzů.</span><span class="sxs-lookup"><span data-stu-id="95eaa-192">You'll do that in the [deploy to IIS](deploying-to-iis.md) and [deploy to production](deploying-to-production.md) tutorials.</span></span>

> [!NOTE]
> <span data-ttu-id="95eaa-193">Vzhledem k tomu, že toto nastavení je v `<appSettings>` prvku, máte jinou alternativu pro určení transformace při nasazování do webových aplikací ve službě Azure App Service viz určení nastavení [Web.config v Azure](#watransforms) dříve v tomto tématu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-193">Because this setting is in the `<appSettings>` element, you have another alternative for specifying the transformation when you're deploying to Web Apps in Azure App Service See [Specifying Web.config settings in Azure](#watransforms) earlier in this topic.</span></span>

## <a name="setting-connection-strings"></a><span data-ttu-id="95eaa-194">Nastavení připojovacích řetězců</span><span class="sxs-lookup"><span data-stu-id="95eaa-194">Setting connection strings</span></span>

<span data-ttu-id="95eaa-195">Přestože výchozí transformační soubor obsahuje příklad, který ukazuje, jak aktualizovat připojovací řetězec, ve většině případů není nutné nastavit transformace připojovacího řetězce, protože v profilu publikování můžete zadat připojovací řetězce.</span><span class="sxs-lookup"><span data-stu-id="95eaa-195">Although the default transform file contains an example that shows how to update a connection string, in most cases you do not need to set up connection string transformations, because you can specify connection strings in the publish profile.</span></span> <span data-ttu-id="95eaa-196">To uděláte v [nasazení do služby IIS](deploying-to-iis.md) a [nasazení do produkčních](deploying-to-production.md) kurzů.</span><span class="sxs-lookup"><span data-stu-id="95eaa-196">You'll do that in the [deploy to IIS](deploying-to-iis.md) and [deploy to production](deploying-to-production.md) tutorials.</span></span>

## <a name="summary"></a><span data-ttu-id="95eaa-197">Souhrn</span><span class="sxs-lookup"><span data-stu-id="95eaa-197">Summary</span></span>

<span data-ttu-id="95eaa-198">Nyní jste udělali co nejvíce s transformacemi *Web.config* před vytvořením profilů publikování a viděli jste náhled toho, co bude v nasazeném souboru Web.config.</span><span class="sxs-lookup"><span data-stu-id="95eaa-198">You have now done as much as you can with *Web.config* transformations before you create the publish profiles, and you've seen a preview of what will be in the deployed Web.config file.</span></span>

![Náhled transformace umístění](web-config-transformations/_static/image8.png)

<span data-ttu-id="95eaa-200">V následujícím kurzu se postaráte o úlohy nastavení nasazení, které vyžadují nastavení vlastností projektu.</span><span class="sxs-lookup"><span data-stu-id="95eaa-200">In the following tutorial, you'll take care of deployment set-up tasks that require setting project properties.</span></span>

## <a name="more-information"></a><span data-ttu-id="95eaa-201">Další informace</span><span class="sxs-lookup"><span data-stu-id="95eaa-201">More Information</span></span>

<span data-ttu-id="95eaa-202">Další informace o tématech, na něž se vztahuje tento kurz, naleznete [v tématu Použití transformací Web.config ke změně nastavení v cílovém souboru Web.config nebo souboru app.config během nasazení](https://go.microsoft.com/fwlink/p/?LinkId=282413#transforms) v mapě obsahu nasazení webu pro visual studio a ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="95eaa-202">For more information about topics covered by this tutorial, see [Using Web.config transformations to change settings in the destination Web.config file or app.config file during deployment](https://go.microsoft.com/fwlink/p/?LinkId=282413#transforms) in the Web Deployment Content Map for Visual Studio and ASP.NET.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="95eaa-203">[Předchozí](preparing-databases.md)
> [další](project-properties.md)</span><span class="sxs-lookup"><span data-stu-id="95eaa-203">[Previous](preparing-databases.md)
[Next](project-properties.md)</span></span>
