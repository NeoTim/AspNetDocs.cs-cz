---
uid: mvc/overview/older-versions/getting-started-with-ef-5-using-mvc-4/advanced-entity-framework-scenarios-for-an-mvc-web-application
title: Pokročilé scénáře Entity Framework pro webovou aplikaci MVC (10 z 10) | Microsoft Docs
author: tdykstra
description: Ukázková webová aplikace společnosti Contoso University ukazuje, jak vytvářet aplikace ASP.NET MVC 4 pomocí Code First Entity Framework 5 a Visual Studio...
ms.author: riande
ms.date: 07/30/2013
ms.assetid: 64906a1d-f734-41cf-9615-ee95f8740996
msc.legacyurl: /mvc/overview/older-versions/getting-started-with-ef-5-using-mvc-4/advanced-entity-framework-scenarios-for-an-mvc-web-application
msc.type: authoredcontent
ms.openlocfilehash: f8f079f6d8ea663c6888456be422a2bae93a4b87
ms.sourcegitcommit: ce28244209db8615bc9bdd576a2e2c88174d318d
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/06/2020
ms.locfileid: "86163453"
---
# <a name="advanced-entity-framework-scenarios-for-an-mvc-web-application-10-of-10"></a><span data-ttu-id="cd0de-103">Pokročilé scénáře Entity Framework pro webovou aplikaci MVC (10 z 10)</span><span class="sxs-lookup"><span data-stu-id="cd0de-103">Advanced Entity Framework Scenarios for an MVC Web Application (10 of 10)</span></span>

<span data-ttu-id="cd0de-104">tím, že [Dykstra](https://github.com/tdykstra)</span><span class="sxs-lookup"><span data-stu-id="cd0de-104">by [Tom Dykstra](https://github.com/tdykstra)</span></span>

[<span data-ttu-id="cd0de-105">Stáhnout dokončený projekt</span><span class="sxs-lookup"><span data-stu-id="cd0de-105">Download Completed Project</span></span>](https://code.msdn.microsoft.com/Getting-Started-with-dd0e2ed8)

> <span data-ttu-id="cd0de-106">Ukázková webová aplikace společnosti Contoso University ukazuje, jak vytvářet aplikace ASP.NET MVC 4 pomocí Code First Entity Framework 5 a Visual Studio 2012.</span><span class="sxs-lookup"><span data-stu-id="cd0de-106">The Contoso University sample web application demonstrates how to create ASP.NET MVC 4 applications using the Entity Framework 5 Code First and Visual Studio 2012.</span></span> <span data-ttu-id="cd0de-107">Informace o řadě kurzů najdete v [prvním kurzu v řadě](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md).</span><span class="sxs-lookup"><span data-stu-id="cd0de-107">For information about the tutorial series, see [the first tutorial in the series](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md).</span></span> <span data-ttu-id="cd0de-108">Řadu kurzů můžete spustit na začátku nebo [si stáhnout počáteční projekt pro tuto kapitolu](building-the-ef5-mvc4-chapter-downloads.md) a začít zde.</span><span class="sxs-lookup"><span data-stu-id="cd0de-108">You can start the tutorial series from the beginning or [download a starter project for this chapter](building-the-ef5-mvc4-chapter-downloads.md) and start here.</span></span>
> 
> > [!NOTE] 
> > 
> > <span data-ttu-id="cd0de-109">Pokud narazíte na problém, který nemůžete vyřešit, [Stáhněte si dokončenou kapitolu](building-the-ef5-mvc4-chapter-downloads.md) a pokuste se problém reprodukován.</span><span class="sxs-lookup"><span data-stu-id="cd0de-109">If you run into a problem you can't resolve, [download the completed chapter](building-the-ef5-mvc4-chapter-downloads.md) and try to reproduce your problem.</span></span> <span data-ttu-id="cd0de-110">Řešení problému můžete obecně najít porovnáním kódu s dokončeným kódem.</span><span class="sxs-lookup"><span data-stu-id="cd0de-110">You can generally find the solution to the problem by comparing your code to the completed code.</span></span> <span data-ttu-id="cd0de-111">Některé běžné chyby a jejich řešení najdete v tématu [chyby a alternativní řešení.](advanced-entity-framework-scenarios-for-an-mvc-web-application.md#errors)</span><span class="sxs-lookup"><span data-stu-id="cd0de-111">For some common errors and how to solve them, see [Errors and Workarounds.](advanced-entity-framework-scenarios-for-an-mvc-web-application.md#errors)</span></span>

<span data-ttu-id="cd0de-112">V předchozím kurzu jste implementovali úložiště a pracovní jednotky vzorů.</span><span class="sxs-lookup"><span data-stu-id="cd0de-112">In the previous tutorial you implemented the repository and unit of work patterns.</span></span> <span data-ttu-id="cd0de-113">Tento kurz se zabývá následujícími tématy:</span><span class="sxs-lookup"><span data-stu-id="cd0de-113">This tutorial covers the following topics:</span></span>

- <span data-ttu-id="cd0de-114">Provádění nezpracovaných dotazů SQL.</span><span class="sxs-lookup"><span data-stu-id="cd0de-114">Performing raw SQL queries.</span></span>
- <span data-ttu-id="cd0de-115">Provádění dotazů bez sledování.</span><span class="sxs-lookup"><span data-stu-id="cd0de-115">Performing no-tracking queries.</span></span>
- <span data-ttu-id="cd0de-116">Zkoumání dotazů odeslaných do databáze.</span><span class="sxs-lookup"><span data-stu-id="cd0de-116">Examining queries sent to the database.</span></span>
- <span data-ttu-id="cd0de-117">Práce se třídami proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="cd0de-117">Working with proxy classes.</span></span>
- <span data-ttu-id="cd0de-118">Zakáže se automatické zjišťování změn.</span><span class="sxs-lookup"><span data-stu-id="cd0de-118">Disabling automatic detection of changes.</span></span>
- <span data-ttu-id="cd0de-119">Zakáže se ověřování při ukládání změn.</span><span class="sxs-lookup"><span data-stu-id="cd0de-119">Disabling validation when saving changes.</span></span>
- [<span data-ttu-id="cd0de-120">Chyby a obejít</span><span class="sxs-lookup"><span data-stu-id="cd0de-120">Errors and Work Arounds</span></span>](#errors)

<span data-ttu-id="cd0de-121">U většiny těchto témat budete pracovat se stránkami, které jste již vytvořili.</span><span class="sxs-lookup"><span data-stu-id="cd0de-121">For most of these topics, you'll work with pages that you already created.</span></span> <span data-ttu-id="cd0de-122">Pokud chcete hromadné aktualizace použít nezpracovaných SQL, vytvoří se nová stránka, která aktualizuje počet kreditů všech kurzů v databázi:</span><span class="sxs-lookup"><span data-stu-id="cd0de-122">To use raw SQL to do bulk updates you'll create a new page that updates the number of credits of all courses in the database:</span></span>

![Update_Course_Credits_initial_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image1.png)

<span data-ttu-id="cd0de-124">A pokud chcete použít dotaz bez sledování, přidejte novou logiku ověřování na stránku pro úpravy oddělení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-124">And to use a no-tracking query you'll add new validation logic to the Department Edit page:</span></span>

![Department_Edit_page_with_duplicate_administrator_error_message](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image2.png)

## <a name="performing-raw-sql-queries"></a><span data-ttu-id="cd0de-126">Provádění nezpracovaných dotazů SQL</span><span class="sxs-lookup"><span data-stu-id="cd0de-126">Performing Raw SQL Queries</span></span>

<span data-ttu-id="cd0de-127">Rozhraní API pro Entity Framework Code First obsahuje metody, které umožňují předat příkazy SQL přímo do databáze.</span><span class="sxs-lookup"><span data-stu-id="cd0de-127">The Entity Framework Code First API includes methods that enable you to pass SQL commands directly to the database.</span></span> <span data-ttu-id="cd0de-128">Máte tyto možnosti:</span><span class="sxs-lookup"><span data-stu-id="cd0de-128">You have the following options:</span></span>

- <span data-ttu-id="cd0de-129">Použijte `DbSet.SqlQuery` metodu pro dotazy, které vracejí typy entit.</span><span class="sxs-lookup"><span data-stu-id="cd0de-129">Use the `DbSet.SqlQuery` method for queries that return entity types.</span></span> <span data-ttu-id="cd0de-130">Vrácené objekty musí být typu očekávaného `DbSet` objektem a automaticky sledovány pomocí kontextu databáze, pokud nevypnete sledování.</span><span class="sxs-lookup"><span data-stu-id="cd0de-130">The returned objects must be of the type expected by the `DbSet` object, and they are automatically tracked by the database context unless you turn tracking off.</span></span> <span data-ttu-id="cd0de-131">(Další informace o metodě naleznete v následující části `AsNoTracking` .)</span><span class="sxs-lookup"><span data-stu-id="cd0de-131">(See the following section about the `AsNoTracking` method.)</span></span>
- <span data-ttu-id="cd0de-132">Použijte `Database.SqlQuery` metodu pro dotazy, které vracejí typy, které nejsou entitami.</span><span class="sxs-lookup"><span data-stu-id="cd0de-132">Use the `Database.SqlQuery` method for queries that return types that aren't entities.</span></span> <span data-ttu-id="cd0de-133">Vrácená data nejsou sledována kontextem databáze, a to i v případě, že použijete tuto metodu k načtení typů entit.</span><span class="sxs-lookup"><span data-stu-id="cd0de-133">The returned data isn't tracked by the database context, even if you use this method to retrieve entity types.</span></span>
- <span data-ttu-id="cd0de-134">Pro příkazy, které nejsou typu Query, použijte [Database.ExecuteSqlCommand](https://msdn.microsoft.com/library/gg679456(v=vs.103).aspx) .</span><span class="sxs-lookup"><span data-stu-id="cd0de-134">Use the [Database.ExecuteSqlCommand](https://msdn.microsoft.com/library/gg679456(v=vs.103).aspx) for non-query commands.</span></span>

<span data-ttu-id="cd0de-135">Jednou z výhod používání Entity Framework je, že se vyhnete tomu, že váš kód je příliš úzce k určité metodě ukládání dat.</span><span class="sxs-lookup"><span data-stu-id="cd0de-135">One of the advantages of using the Entity Framework is that it avoids tying your code too closely to a particular method of storing data.</span></span> <span data-ttu-id="cd0de-136">Provede to tím, že vygeneruje dotazy a příkazy SQL za vás, což vám taky zabrání v jejich psaní.</span><span class="sxs-lookup"><span data-stu-id="cd0de-136">It does this by generating SQL queries and commands for you, which also frees you from having to write them yourself.</span></span> <span data-ttu-id="cd0de-137">Existují však výjimečné scénáře, pokud potřebujete spustit konkrétní dotazy SQL, které jste vytvořili ručně, a tyto metody umožňují zpracovávat takové výjimky.</span><span class="sxs-lookup"><span data-stu-id="cd0de-137">But there are exceptional scenarios when you need to run specific SQL queries that you have manually created, and these methods make it possible for you to handle such exceptions.</span></span>

<span data-ttu-id="cd0de-138">Jak je vždy true při provádění příkazů SQL ve webové aplikaci, je nutné podniknout preventivní opatření k ochraně vašeho webu před útoky prostřednictvím injektáže SQL.</span><span class="sxs-lookup"><span data-stu-id="cd0de-138">As is always true when you execute SQL commands in a web application, you must take precautions to protect your site against SQL injection attacks.</span></span> <span data-ttu-id="cd0de-139">Jedním ze způsobů, jak to provést, je použít parametrizované dotazy, aby se zajistilo, že řetězce odeslané webovou stránkou nejde interpretovat jako příkazy SQL.</span><span class="sxs-lookup"><span data-stu-id="cd0de-139">One way to do that is to use parameterized queries to make sure that strings submitted by a web page can't be interpreted as SQL commands.</span></span> <span data-ttu-id="cd0de-140">V tomto kurzu použijete parametrizované dotazy při integraci vstupu uživatele do dotazu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-140">In this tutorial you'll use parameterized queries when integrating user input into a query.</span></span>

### <a name="calling-a-query-that-returns-entities"></a><span data-ttu-id="cd0de-141">Volání dotazu, který vrací entity</span><span class="sxs-lookup"><span data-stu-id="cd0de-141">Calling a Query that Returns Entities</span></span>

<span data-ttu-id="cd0de-142">Předpokládejme, že chcete, `GenericRepository` aby třída poskytovala další filtrování a flexibilitu řazení bez nutnosti vytvořit odvozenou třídu s dalšími metodami.</span><span class="sxs-lookup"><span data-stu-id="cd0de-142">Suppose you want the `GenericRepository` class to provide additional filtering and sorting flexibility without requiring that you create a derived class with additional methods.</span></span> <span data-ttu-id="cd0de-143">Jedním ze způsobů, jak toho dosáhnout, je přidat metodu, která přijímá dotaz SQL.</span><span class="sxs-lookup"><span data-stu-id="cd0de-143">One way to achieve that would be to add a method that accepts a SQL query.</span></span> <span data-ttu-id="cd0de-144">V kontroleru pak můžete určit jakýkoli druh filtrování nebo řazení, jako je například `Where` klauzule, která závisí na spojeních nebo poddotazu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-144">You could then specify any kind of filtering or sorting you want in the controller, such as a `Where` clause that depends on a joins or a subquery.</span></span> <span data-ttu-id="cd0de-145">V této části se dozvíte, jak implementovat takovou metodu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-145">In this section you'll see how to implement such a method.</span></span>

<span data-ttu-id="cd0de-146">Vytvořte `GetWithRawSql` metodu přidáním následujícího kódu do *GenericRepository.cs*:</span><span class="sxs-lookup"><span data-stu-id="cd0de-146">Create the `GetWithRawSql` method by adding the following code to *GenericRepository.cs*:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample1.cs)]

<span data-ttu-id="cd0de-147">V *CourseController.cs*zavolejte novou metodu z `Details` metody, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="cd0de-147">In *CourseController.cs*, call the new method from the `Details` method, as shown in the following example:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample2.cs)]

<span data-ttu-id="cd0de-148">V tomto případě jste mohli použít `GetByID` metodu, ale používáte `GetWithRawSql` metodu k ověření, že `GetWithRawSQL` metoda funguje.</span><span class="sxs-lookup"><span data-stu-id="cd0de-148">In this case you could have used the `GetByID` method, but you're using the `GetWithRawSql` method to verify that the `GetWithRawSQL` method works.</span></span>

<span data-ttu-id="cd0de-149">Spusťte stránku podrobností, abyste ověřili, že výběrový dotaz funguje (vyberte kartu **kurz** a pak **Podrobnosti** o jednom kurzu).</span><span class="sxs-lookup"><span data-stu-id="cd0de-149">Run the Details page to verify that the select query works (select the **Course** tab and then **Details** for one course).</span></span>

![Course_Details_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image3.png)

### <a name="calling-a-query-that-returns-other-types-of-objects"></a><span data-ttu-id="cd0de-151">Volání dotazu, který vrací jiné typy objektů</span><span class="sxs-lookup"><span data-stu-id="cd0de-151">Calling a Query that Returns Other Types of Objects</span></span>

<span data-ttu-id="cd0de-152">Dříve jste vytvořili tabulku statistik studentů pro stránku o produktu, která ukázala počet studentů pro každé datum registrace.</span><span class="sxs-lookup"><span data-stu-id="cd0de-152">Earlier you created a student statistics grid for the About page that showed the number of students for each enrollment date.</span></span> <span data-ttu-id="cd0de-153">Kód, který je v *HomeController.cs* , používá LINQ:</span><span class="sxs-lookup"><span data-stu-id="cd0de-153">The code that does this in *HomeController.cs* uses LINQ:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample3.cs)]

<span data-ttu-id="cd0de-154">Předpokládejme, že chcete napsat kód, který načte tato data přímo v SQL místo použití LINQ.</span><span class="sxs-lookup"><span data-stu-id="cd0de-154">Suppose you want to write the code that retrieves this data directly in SQL rather than using LINQ.</span></span> <span data-ttu-id="cd0de-155">K tomu je nutné spustit dotaz, který vrací něco jiného než objekty entity, což znamená, že je nutné použít `Database.SqlQuery` metodu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-155">To do that you need to run a query that returns something other than entity objects, which means you need to use the `Database.SqlQuery` method.</span></span>

<span data-ttu-id="cd0de-156">V *HomeController.cs*nahraďte příkaz LINQ v `About` metodě následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cd0de-156">In *HomeController.cs*, replace the LINQ statement in the `About` method with the following code:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample4.cs)]

<span data-ttu-id="cd0de-157">Spusťte stránku o produktu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-157">Run the About page.</span></span> <span data-ttu-id="cd0de-158">Zobrazuje stejná data jako předtím.</span><span class="sxs-lookup"><span data-stu-id="cd0de-158">It displays the same data it did before.</span></span>

![About_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image4.png)

### <a name="calling-an-update-query"></a><span data-ttu-id="cd0de-160">Volání aktualizačního dotazu</span><span class="sxs-lookup"><span data-stu-id="cd0de-160">Calling an Update Query</span></span>

<span data-ttu-id="cd0de-161">Předpokládejme, že správci služby contoso University chtějí v databázi provádět hromadné změny, jako je třeba Změna počtu kreditů pro každý kurz.</span><span class="sxs-lookup"><span data-stu-id="cd0de-161">Suppose Contoso University administrators want to be able to perform bulk changes in the database, such as changing the number of credits for every course.</span></span> <span data-ttu-id="cd0de-162">Pokud má univerzita velký počet kurzů, je třeba je neefektivně načíst jako entity a jednotlivě je měnit.</span><span class="sxs-lookup"><span data-stu-id="cd0de-162">If the university has a large number of courses, it would be inefficient to retrieve them all as entities and change them individually.</span></span> <span data-ttu-id="cd0de-163">V této části implementujete webovou stránku, která uživateli umožní zadat faktor, podle kterého se má změnit počet kreditů pro všechny kurzy, a provedete změnu provedením `UPDATE` příkazu SQL.</span><span class="sxs-lookup"><span data-stu-id="cd0de-163">In this section you'll implement a web page that allows the user to specify a factor by which to change the number of credits for all courses, and you'll make the change by executing a SQL `UPDATE` statement.</span></span> <span data-ttu-id="cd0de-164">Webová stránka bude vypadat jako na následujícím obrázku:</span><span class="sxs-lookup"><span data-stu-id="cd0de-164">The web page will look like the following illustration:</span></span>

![Update_Course_Credits_initial_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image5.png)

<span data-ttu-id="cd0de-166">V předchozím kurzu jste použili obecné úložiště ke čtení a aktualizaci `Course` entit v `Course` řadiči.</span><span class="sxs-lookup"><span data-stu-id="cd0de-166">In the previous tutorial you used the generic repository to read and update `Course` entities in the `Course` controller.</span></span> <span data-ttu-id="cd0de-167">Pro tuto operaci hromadné aktualizace je potřeba vytvořit novou metodu úložiště, která není v obecném úložišti.</span><span class="sxs-lookup"><span data-stu-id="cd0de-167">For this bulk update operation, you need to create a new repository method that isn't in the generic repository.</span></span> <span data-ttu-id="cd0de-168">K tomu vytvoříte vyhrazenou `CourseRepository` třídu, která je odvozena od `GenericRepository` třídy.</span><span class="sxs-lookup"><span data-stu-id="cd0de-168">To do that, you'll create a dedicated `CourseRepository` class that derives from the `GenericRepository` class.</span></span>

<span data-ttu-id="cd0de-169">Ve složce *dal* vytvořte *CourseRepository.cs* a nahraďte existující kód následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cd0de-169">In the *DAL* folder, create *CourseRepository.cs* and replace the existing code with the following code:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample5.cs)]

<span data-ttu-id="cd0de-170">V *UnitOfWork.cs*změňte `Course` Typ úložiště z na. `GenericRepository<Course>``CourseRepository:`</span><span class="sxs-lookup"><span data-stu-id="cd0de-170">In *UnitOfWork.cs*, change the `Course` repository type from `GenericRepository<Course>` to `CourseRepository:`</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample6.cs)]

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample7.cs)]

<span data-ttu-id="cd0de-171">Do *CourseController.cs*přidejte `UpdateCourseCredits` metodu:</span><span class="sxs-lookup"><span data-stu-id="cd0de-171">In *CourseController.cs*, add an `UpdateCourseCredits` method:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample8.cs)]

<span data-ttu-id="cd0de-172">Tato metoda bude použita pro i `HttpGet` `HttpPost` .</span><span class="sxs-lookup"><span data-stu-id="cd0de-172">This method will be used for both `HttpGet` and `HttpPost`.</span></span> <span data-ttu-id="cd0de-173">Když je `HttpGet` `UpdateCourseCredits` metoda spuštěna, `multiplier` proměnná bude null a zobrazení zobrazí prázdné textové pole a tlačítko Odeslat, jak je znázorněno na předchozím obrázku.</span><span class="sxs-lookup"><span data-stu-id="cd0de-173">When the `HttpGet` `UpdateCourseCredits` method runs, the `multiplier` variable will be null and the view will display an empty text box and a submit button, as shown in the preceding illustration.</span></span>

<span data-ttu-id="cd0de-174">Po kliknutí na tlačítko **aktualizovat** a `HttpPost` spuštění metody `multiplier` bude v textovém poli zadána hodnota.</span><span class="sxs-lookup"><span data-stu-id="cd0de-174">When the **Update** button is clicked and the `HttpPost` method runs, `multiplier` will have the value entered in the text box.</span></span> <span data-ttu-id="cd0de-175">Kód pak zavolá metodu úložiště `UpdateCourseCredits` , která vrátí počet ovlivněných řádků a tato hodnota je uložena v `ViewBag` objektu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-175">The code then calls the repository `UpdateCourseCredits` method, which returns the number of affected rows, and that value is stored in the `ViewBag` object.</span></span> <span data-ttu-id="cd0de-176">Když zobrazení obdrží počet ovlivněných řádků v `ViewBag` objektu, zobrazí toto číslo místo textového pole a tlačítka Odeslat, jak je znázorněno na následujícím obrázku:</span><span class="sxs-lookup"><span data-stu-id="cd0de-176">When the view receives the number of affected rows in the `ViewBag` object, it displays that number instead of the text box and submit button, as shown in the following illustration:</span></span>

![Update_Course_Credits_rows_affected_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image6.png)

<span data-ttu-id="cd0de-178">Vytvořte zobrazení ve složce *Views\Course* na stránce aktualizovat kredity kurzu:</span><span class="sxs-lookup"><span data-stu-id="cd0de-178">Create a view in the *Views\Course* folder for the Update Course Credits page:</span></span>

![Add_View_dialog_box_for_Update_Course_Credits](https://asp.net/media/2578203/Windows-Live-Writer_Advanced-Entity-Framework-Scenarios-for-_CEF8_Add_View_dialog_box_for_Update_Course_Credits.png)

<span data-ttu-id="cd0de-180">V *Views\Course\UpdateCourseCredits.cshtml*nahraďte kód šablony následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cd0de-180">In *Views\Course\UpdateCourseCredits.cshtml*, replace the template code with the following code:</span></span>

[!code-cshtml[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample9.cshtml)]

<span data-ttu-id="cd0de-181">Spusťte `UpdateCourseCredits` metodu tak, že vyberete kartu **kurzy** a pak na konec adresy URL v adresním řádku prohlížeče přidáte "/UpdateCourseCredits" (například: `http://localhost:50205/Course/UpdateCourseCredits` ).</span><span class="sxs-lookup"><span data-stu-id="cd0de-181">Run the `UpdateCourseCredits` method by selecting the **Courses** tab, then adding "/UpdateCourseCredits" to the end of the URL in the browser's address bar (for example: `http://localhost:50205/Course/UpdateCourseCredits`).</span></span> <span data-ttu-id="cd0de-182">Do textového pole zadejte číslo:</span><span class="sxs-lookup"><span data-stu-id="cd0de-182">Enter a number in the text box:</span></span>

![Update_Course_Credits_initial_page_with_2_entered](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image7.png)

<span data-ttu-id="cd0de-184">Klikněte na **Aktualizovat**.</span><span class="sxs-lookup"><span data-stu-id="cd0de-184">Click **Update**.</span></span> <span data-ttu-id="cd0de-185">Zobrazí se počet ovlivněných řádků:</span><span class="sxs-lookup"><span data-stu-id="cd0de-185">You see the number of rows affected:</span></span>

![Update_Course_Credits_rows_affected_page](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image8.png)

<span data-ttu-id="cd0de-187">Kliknutím na **zpět na seznam** zobrazíte seznam kurzů s revidovaným počtem kreditů.</span><span class="sxs-lookup"><span data-stu-id="cd0de-187">Click **Back to List** to see the list of courses with the revised number of credits.</span></span>

![Courses_Index_page_showing_revised_credits](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image9.png)

<span data-ttu-id="cd0de-189">Další informace o nezpracovaných dotazech SQL najdete v tématu [nezpracované dotazy SQL](https://blogs.msdn.com/b/adonet/archive/2011/02/04/using-dbcontext-in-ef-feature-ctp5-part-10-raw-sql-queries.aspx) na blogu týmu Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="cd0de-189">For more information about raw SQL queries, see [Raw SQL Queries](https://blogs.msdn.com/b/adonet/archive/2011/02/04/using-dbcontext-in-ef-feature-ctp5-part-10-raw-sql-queries.aspx) on the Entity Framework team blog.</span></span>

## <a name="no-tracking-queries"></a><span data-ttu-id="cd0de-190">Žádné dotazy pro sledování</span><span class="sxs-lookup"><span data-stu-id="cd0de-190">No-Tracking Queries</span></span>

<span data-ttu-id="cd0de-191">Když kontext databáze načte řádky databáze a vytvoří objekty entity, které je reprezentují, ve výchozím nastavení udržuje přehled o tom, zda jsou entity v paměti synchronizovány s obsahem databáze.</span><span class="sxs-lookup"><span data-stu-id="cd0de-191">When a database context retrieves database rows and creates entity objects that represent them, by default it keeps track of whether the entities in memory are in sync with what's in the database.</span></span> <span data-ttu-id="cd0de-192">Data v paměti fungují jako mezipaměť a používají se při aktualizaci entity.</span><span class="sxs-lookup"><span data-stu-id="cd0de-192">The data in memory acts as a cache and is used when you update an entity.</span></span> <span data-ttu-id="cd0de-193">Toto ukládání do mezipaměti je často zbytečné v rámci webové aplikace, protože instance kontextu jsou obvykle krátkodobé – neuvolňuje (nové je vytvořeno a odstraněno pro každý požadavek) a kontext, který čte entitu, je obvykle uvolněn předtím, než se entita znovu použije.</span><span class="sxs-lookup"><span data-stu-id="cd0de-193">This caching is often unnecessary in a web application because context instances are typically short-lived (a new one is created and disposed for each request) and the context that reads an entity is typically disposed before that entity is used again.</span></span>

<span data-ttu-id="cd0de-194">Můžete určit, zda kontext sleduje objekty entit pro dotaz pomocí `AsNoTracking` metody.</span><span class="sxs-lookup"><span data-stu-id="cd0de-194">You can specify whether the context tracks entity objects for a query by using the `AsNoTracking` method.</span></span> <span data-ttu-id="cd0de-195">Mezi obvyklé scénáře, které byste mohli chtít udělat, patří následující:</span><span class="sxs-lookup"><span data-stu-id="cd0de-195">Typical scenarios in which you might want to do that include the following:</span></span>

- <span data-ttu-id="cd0de-196">Dotaz načte takový velký objem dat, který vypne sledování, může výrazně zvýšit výkon.</span><span class="sxs-lookup"><span data-stu-id="cd0de-196">The query retrieves such a large volume of data that turning off tracking might noticeably enhance performance.</span></span>
- <span data-ttu-id="cd0de-197">Chcete připojit entitu, abyste ji mohli aktualizovat, ale dříve jste načetli stejnou entitu pro jiný účel.</span><span class="sxs-lookup"><span data-stu-id="cd0de-197">You want to attach an entity in order to update it, but you earlier retrieved the same entity for a different purpose.</span></span> <span data-ttu-id="cd0de-198">Vzhledem k tomu, že entita je již sledována kontextem databáze, nelze připojit entitu, kterou chcete změnit.</span><span class="sxs-lookup"><span data-stu-id="cd0de-198">Because the entity is already being tracked by the database context, you can't attach the entity that you want to change.</span></span> <span data-ttu-id="cd0de-199">Jedním ze způsobů, jak zabránit tomuto chování, je použití `AsNoTracking` Možnosti s dřívějším dotazem.</span><span class="sxs-lookup"><span data-stu-id="cd0de-199">One way to prevent this from happening is to use the `AsNoTracking` option with the earlier query.</span></span>

<span data-ttu-id="cd0de-200">V této části budete implementovat obchodní logiku, která ilustruje druhý z těchto scénářů.</span><span class="sxs-lookup"><span data-stu-id="cd0de-200">In this section you'll implement business logic that illustrates the second of these scenarios.</span></span> <span data-ttu-id="cd0de-201">Konkrétně vynutili obchodní pravidlo, které říká, že instruktor nemůže být správcem více oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-201">Specifically, you'll enforce a business rule that says that an instructor can't be the administrator of more than one department.</span></span>

<span data-ttu-id="cd0de-202">V *DepartmentController.cs*přidejte novou metodu, kterou můžete volat z metod a, aby se `Edit` `Create` zajistilo, že žádný ze dvou oddělení nemá stejného správce:</span><span class="sxs-lookup"><span data-stu-id="cd0de-202">In *DepartmentController.cs*, add a new method that you can call from the `Edit` and `Create` methods to make sure that no two departments have the same administrator:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample10.cs)]

<span data-ttu-id="cd0de-203">Přidejte kód do `try` bloku `HttpPost` `Edit` metody pro volání této nové metody, pokud nedošlo k chybám ověření.</span><span class="sxs-lookup"><span data-stu-id="cd0de-203">Add code in the `try` block of the `HttpPost` `Edit` method to call this new method if there are no validation errors.</span></span> <span data-ttu-id="cd0de-204">`try`Blok teď vypadá jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="cd0de-204">The `try` block now looks like the following example:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample11.cs?highlight=9-12)]

<span data-ttu-id="cd0de-205">Spusťte stránku pro úpravy oddělení a pokuste se změnit správce oddělení na instruktora, který je již správcem jiného oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-205">Run the Department Edit page and try to change a department's administrator to an instructor who is already the administrator of a different department.</span></span> <span data-ttu-id="cd0de-206">Zobrazí se očekávaná chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-206">You get the expected error message:</span></span>

![Department_Edit_page_with_duplicate_administrator_error_message](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image10.png)

<span data-ttu-id="cd0de-208">Nyní spusťte znovu stránku Upravit oddělení a tentokrát změňte množství **rozpočtu** .</span><span class="sxs-lookup"><span data-stu-id="cd0de-208">Now run the Department Edit page again and this time change the **Budget** amount.</span></span> <span data-ttu-id="cd0de-209">Když kliknete na **Uložit**, zobrazí se chybová stránka:</span><span class="sxs-lookup"><span data-stu-id="cd0de-209">When you click **Save**, you see an error page:</span></span>

![Department_Edit_page_with_object_state_manager_error_message](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image11.png)

<span data-ttu-id="cd0de-211">Chybová zpráva výjimky je "", k nimž `An object with the same key already exists in the ObjectStateManager. The ObjectStateManager cannot track multiple objects with the same key.` došlo z důvodu následující posloupnosti událostí:</span><span class="sxs-lookup"><span data-stu-id="cd0de-211">The exception error message is "`An object with the same key already exists in the ObjectStateManager. The ObjectStateManager cannot track multiple objects with the same key.`" This happened because of the following sequence of events:</span></span>

- <span data-ttu-id="cd0de-212">`Edit`Metoda volá `ValidateOneAdministratorAssignmentPerInstructor` metodu, která načte všechna oddělení, která mají Jan Abercrombie jako správce.</span><span class="sxs-lookup"><span data-stu-id="cd0de-212">The `Edit` method calls the `ValidateOneAdministratorAssignmentPerInstructor` method, which retrieves all departments that have Kim Abercrombie as their administrator.</span></span> <span data-ttu-id="cd0de-213">To způsobuje, že se v anglickém oddělení přečte.</span><span class="sxs-lookup"><span data-stu-id="cd0de-213">That causes the English department to be read.</span></span> <span data-ttu-id="cd0de-214">Vzhledem k tomu, že se jedná o upravované oddělení, není hlášena žádná chyba.</span><span class="sxs-lookup"><span data-stu-id="cd0de-214">Because that's the department being edited, no error is reported.</span></span> <span data-ttu-id="cd0de-215">Výsledkem této operace čtení však je, že entita pro angličtinu, která byla načtena z databáze, je nyní sledována kontextem databáze.</span><span class="sxs-lookup"><span data-stu-id="cd0de-215">As a result of this read operation, however, the English department entity that was read from the database is now being tracked by the database context.</span></span>
- <span data-ttu-id="cd0de-216">`Edit`Metoda se pokusí nastavit `Modified` příznak pro entitu v rámci anglického oddělení vytvořenou pořadačem modelu MVC, ale to se nepovede, protože kontext už sleduje entitu pro anglické oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-216">The `Edit` method tries to set the `Modified` flag on the English department entity created by the MVC model binder, but that fails because the context is already tracking an entity for the English department.</span></span>

<span data-ttu-id="cd0de-217">Jedním z řešení tohoto problému je zachovat kontext ze sledování entit oddělení v paměti načtených ověřovacím dotazem.</span><span class="sxs-lookup"><span data-stu-id="cd0de-217">One solution to this problem is to keep the context from tracking in-memory department entities retrieved by the validation query.</span></span> <span data-ttu-id="cd0de-218">K tomu nehrozí žádná nevýhoda, protože tuto entitu neaktualizujete ani ji nebudete moct znovu číst způsobem, který by byl z paměti v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="cd0de-218">There's no disadvantage to doing this, because you won't be updating this entity or reading it again in a way that would benefit from it being cached in memory.</span></span>

<span data-ttu-id="cd0de-219">V *DepartmentController.cs*v `ValidateOneAdministratorAssignmentPerInstructor` metodě určete bez sledování, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="cd0de-219">In *DepartmentController.cs*, in the `ValidateOneAdministratorAssignmentPerInstructor` method, specify no tracking, as shown in the following:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample12.cs?highlight=4)]

<span data-ttu-id="cd0de-220">Opakujte pokus o úpravu částky **rozpočtu** oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-220">Repeat your attempt to edit the **Budget** amount of a department.</span></span> <span data-ttu-id="cd0de-221">Tentokrát je operace úspěšná a lokalita se vrátí podle očekávání na stránku rejstřík oddělení, kde zobrazuje revidovanou hodnotu rozpočtu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-221">This time the operation is successful, and the site returns as expected to the Departments Index page, showing the revised budget value.</span></span>

## <a name="examining-queries-sent-to-the-database"></a><span data-ttu-id="cd0de-222">Zkoumání dotazů odeslaných do databáze</span><span class="sxs-lookup"><span data-stu-id="cd0de-222">Examining Queries Sent to the Database</span></span>

<span data-ttu-id="cd0de-223">Někdy je užitečné, abyste si mohli prohlédnout skutečné dotazy SQL, které se odesílají do databáze.</span><span class="sxs-lookup"><span data-stu-id="cd0de-223">Sometimes it's helpful to be able to see the actual SQL queries that are sent to the database.</span></span> <span data-ttu-id="cd0de-224">Chcete-li to provést, můžete prostudovat proměnnou dotazu v ladicím programu nebo volat `ToString` metodu dotazu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-224">To do this, you can examine a query variable in the debugger or call the query's `ToString` method.</span></span> <span data-ttu-id="cd0de-225">Pokud to chcete vyzkoušet, podívejte se na jednoduchý dotaz a podívejte se na to, co se děje, když přidáte možnosti, jako je Eager načítání, filtrování a řazení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-225">To try this out, you'll look at a simple query and then look at what happens to it as you add options such eager loading, filtering, and sorting.</span></span>

<span data-ttu-id="cd0de-226">V části *Controllers/CourseController*nahraďte `Index` metodu následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cd0de-226">In *Controllers/CourseController*, replace the `Index` method with the following code:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample13.cs?highlight=3)]

<span data-ttu-id="cd0de-227">Nyní nastavte zarážku v *GenericRepository.cs* na `return query.ToList();` a `return orderBy(query).ToList();` příkazy `Get` metody.</span><span class="sxs-lookup"><span data-stu-id="cd0de-227">Now set a breakpoint in *GenericRepository.cs* on the `return query.ToList();` and the `return orderBy(query).ToList();` statements of the `Get` method.</span></span> <span data-ttu-id="cd0de-228">Spusťte projekt v režimu ladění a vyberte stránku index kurzu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-228">Run the project in debug mode and select the Course Index page.</span></span> <span data-ttu-id="cd0de-229">Když kód dosáhne zarážky, prověřte `query` proměnnou.</span><span class="sxs-lookup"><span data-stu-id="cd0de-229">When the code reaches the breakpoint, examine the `query` variable.</span></span> <span data-ttu-id="cd0de-230">Zobrazí se dotaz, který se odešle do SQL Server.</span><span class="sxs-lookup"><span data-stu-id="cd0de-230">You see the query that's sent to SQL Server.</span></span> <span data-ttu-id="cd0de-231">Jedná se o jednoduchý `Select` příkaz:</span><span class="sxs-lookup"><span data-stu-id="cd0de-231">It's a simple `Select` statement:</span></span>

[!code-json[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample14.json)]

![](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image12.png)

<span data-ttu-id="cd0de-232">Dotazy mohou být příliš dlouhé pro zobrazení v ladicích oknech v aplikaci Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="cd0de-232">Queries can be too long to display in the debugging windows in Visual Studio.</span></span> <span data-ttu-id="cd0de-233">Chcete-li zobrazit celý dotaz, můžete zkopírovat hodnotu proměnné a vložit ji do textového editoru:</span><span class="sxs-lookup"><span data-stu-id="cd0de-233">To see the entire query, you can copy the variable value and paste it into a text editor:</span></span>

![Copy_value_of_variable_in_debug_mode](https://asp.net/media/2578239/Windows-Live-Writer_Advanced-Entity-Framework-Scenarios-for-_CEF8_Copy_value_of_variable_in_debug_mode_0902a2b1-b799-47a6-9b4b-f266c79d83c1.png)

<span data-ttu-id="cd0de-235">Nyní přidáte rozevírací seznam na stránku indexu kurzu, aby uživatelé mohli filtrovat konkrétní oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-235">Now you'll add a drop-down list to the Course Index page so that users can filter for a particular department.</span></span> <span data-ttu-id="cd0de-236">Kurzy seřadíte podle názvu a zadáte Eager načítání pro `Department` navigační vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cd0de-236">You'll sort the courses by title, and you'll specify eager loading for the `Department` navigation property.</span></span> <span data-ttu-id="cd0de-237">V *CourseController.cs*nahraďte `Index` metodu následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="cd0de-237">In *CourseController.cs*, replace the `Index` method with the following code:</span></span>

[!code-csharp[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample15.cs)]

<span data-ttu-id="cd0de-238">Metoda přijímá vybranou hodnotu rozevíracího seznamu v `SelectedDepartment` parametru.</span><span class="sxs-lookup"><span data-stu-id="cd0de-238">The method receives the selected value of the drop-down list in the `SelectedDepartment` parameter.</span></span> <span data-ttu-id="cd0de-239">Pokud není nic vybráno, bude mít tento parametr hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="cd0de-239">If nothing is selected, this parameter will be null.</span></span>

<span data-ttu-id="cd0de-240">`SelectList`Do zobrazení rozevíracího seznamu se předává kolekce obsahující všechna oddělení.</span><span class="sxs-lookup"><span data-stu-id="cd0de-240">A `SelectList` collection containing all departments is passed to the view for the drop-down list.</span></span> <span data-ttu-id="cd0de-241">Parametry předané `SelectList` konstruktoru určují název pole hodnoty, název textového pole a vybranou položku.</span><span class="sxs-lookup"><span data-stu-id="cd0de-241">The parameters passed to the `SelectList` constructor specify the value field name, the text field name, and the selected item.</span></span>

<span data-ttu-id="cd0de-242">Pro `Get` metodu `Course` úložiště kód určuje výraz filtru, pořadí řazení a Eager načítání pro `Department` navigační vlastnost.</span><span class="sxs-lookup"><span data-stu-id="cd0de-242">For the `Get` method of the `Course` repository, the code specifies a filter expression, a sort order, and eager loading for the `Department` navigation property.</span></span> <span data-ttu-id="cd0de-243">Výraz filtru vždy vrátí hodnotu `true` , pokud není nic vybráno v rozevíracím seznamu (tj `SelectedDepartment` . je null).</span><span class="sxs-lookup"><span data-stu-id="cd0de-243">The filter expression always returns `true` if nothing is selected in the drop-down list (that is, `SelectedDepartment` is null).</span></span>

<span data-ttu-id="cd0de-244">V *Views\Course\Index.cshtml*bezprostředně před počáteční `table` značkou přidejte následující kód k vytvoření rozevíracího seznamu a tlačítko Odeslat:</span><span class="sxs-lookup"><span data-stu-id="cd0de-244">In *Views\Course\Index.cshtml*, immediately before the opening `table` tag, add the following code to create the drop-down list and a submit button:</span></span>

[!code-cshtml[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample16.cshtml)]

<span data-ttu-id="cd0de-245">I když se zarážky ve třídě pořád nastavují `GenericRepository` , spusťte stránku index kurzu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-245">With the breakpoints still set in the `GenericRepository` class, run the Course Index page.</span></span> <span data-ttu-id="cd0de-246">Pokračujte v první dvou případech, kdy kód narazí na zarážku, aby se stránka zobrazila v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="cd0de-246">Continue through the first two times that the code hits a breakpoint, so that the page is displayed in the browser.</span></span> <span data-ttu-id="cd0de-247">V rozevíracím seznamu vyberte oddělení a klikněte na **Filtr**:</span><span class="sxs-lookup"><span data-stu-id="cd0de-247">Select a department from the drop-down list and click **Filter**:</span></span>

![Course_Index_page_with_department_selected](advanced-entity-framework-scenarios-for-an-mvc-web-application/_static/image13.png)

<span data-ttu-id="cd0de-249">Tentokrát bude první zarážka pro dotaz oddělení v rozevíracím seznamu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-249">This time the first breakpoint will be for the departments query for the drop-down list.</span></span> <span data-ttu-id="cd0de-250">Přeskočí tuto proměnnou a zobrazí `query` se, když kód příště dosáhne zarážky, aby se zobrazila informace o tom, jaký `Course` dotaz teď vypadá jako.</span><span class="sxs-lookup"><span data-stu-id="cd0de-250">Skip that and view the `query` variable the next time the code reaches the breakpoint in order to see what the `Course` query now looks like.</span></span> <span data-ttu-id="cd0de-251">Uvidíte něco podobného jako následující:</span><span class="sxs-lookup"><span data-stu-id="cd0de-251">You'll see something like the following:</span></span>

[!code-json[Main](advanced-entity-framework-scenarios-for-an-mvc-web-application/samples/sample17.json)]

<span data-ttu-id="cd0de-252">Vidíte, že dotaz je nyní `JOIN` dotazem, který načítá `Department` data společně s `Course` daty a obsahuje `WHERE` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="cd0de-252">You can see that the query is now a `JOIN` query that loads `Department` data along with the `Course` data, and that it includes a `WHERE` clause.</span></span>

<a id="proxyclasses"></a>

## <a name="working-with-proxy-classes"></a><span data-ttu-id="cd0de-253">Práce s třídami proxy</span><span class="sxs-lookup"><span data-stu-id="cd0de-253">Working with Proxy Classes</span></span>

<span data-ttu-id="cd0de-254">Když Entity Framework vytvoří instance entit (například při spuštění dotazu), často je vytvoří jako instance dynamicky generovaného odvozeného typu, který funguje jako proxy pro entitu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-254">When the Entity Framework creates entity instances (for example, when you execute a query), it often creates them as instances of a dynamically generated derived type that acts as a proxy for the entity.</span></span> <span data-ttu-id="cd0de-255">Tento proxy Přepisuje některé virtuální vlastnosti entity, aby mohl vkládat háky pro provádění akcí automaticky při získání k vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="cd0de-255">This proxy overrides some virtual properties of the entity to insert hooks for performing actions automatically when the property is accessed.</span></span> <span data-ttu-id="cd0de-256">Například tento mechanismus slouží k podpoře opožděného načítání vztahů.</span><span class="sxs-lookup"><span data-stu-id="cd0de-256">For example, this mechanism is used to support lazy loading of relationships.</span></span>

<span data-ttu-id="cd0de-257">Ve většině případů nemusíte znát použití proxy serverů, ale existují výjimky:</span><span class="sxs-lookup"><span data-stu-id="cd0de-257">Most of the time you don't need to be aware of this use of proxies, but there are exceptions:</span></span>

- <span data-ttu-id="cd0de-258">V některých případech můžete chtít zabránit Entity Framework v vytváření instancí proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="cd0de-258">In some scenarios you might want to prevent the Entity Framework from creating proxy instances.</span></span> <span data-ttu-id="cd0de-259">Například serializace instancí, které nejsou proxy, může být efektivnější než serializace instancí proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="cd0de-259">For example, serializing non-proxy instances might be more efficient than serializing proxy instances.</span></span>
- <span data-ttu-id="cd0de-260">Při vytváření instance třídy entity pomocí `new` operátoru nezískáte instanci proxy.</span><span class="sxs-lookup"><span data-stu-id="cd0de-260">When you instantiate an entity class using the `new` operator, you don't get a proxy instance.</span></span> <span data-ttu-id="cd0de-261">To znamená, že nezískáte funkce, jako je opožděné načítání a automatické sledování změn.</span><span class="sxs-lookup"><span data-stu-id="cd0de-261">This means you don't get functionality such as lazy loading and automatic change tracking.</span></span> <span data-ttu-id="cd0de-262">Obvykle je to v pořádku. obecně nepotřebujete opožděné načítání, protože vytváříte novou entitu, která není v databázi, a obecně nepotřebujete sledování změn, pokud entitu výslovně označíte jako `Added` .</span><span class="sxs-lookup"><span data-stu-id="cd0de-262">This is typically okay; you generally don't need lazy loading, because you're creating a new entity that isn't in the database, and you generally don't need change tracking if you're explicitly marking the entity as `Added`.</span></span> <span data-ttu-id="cd0de-263">Pokud však potřebujete opožděné načítání a potřebujete sledování změn, můžete vytvořit nové instance entit s proxy objekty pomocí `Create` metody `DbSet` třídy.</span><span class="sxs-lookup"><span data-stu-id="cd0de-263">However, if you do need lazy loading and you need change tracking, you can create new entity instances with proxies using the `Create` method of the `DbSet` class.</span></span>
- <span data-ttu-id="cd0de-264">Je možné, že budete chtít z typu proxy získat skutečný typ entity.</span><span class="sxs-lookup"><span data-stu-id="cd0de-264">You might want to get an actual entity type from a proxy type.</span></span> <span data-ttu-id="cd0de-265">Pomocí `GetObjectType` metody `ObjectContext` třídy můžete získat skutečný typ entity instance typu proxy serveru.</span><span class="sxs-lookup"><span data-stu-id="cd0de-265">You can use the `GetObjectType` method of the `ObjectContext` class to get the actual entity type of a proxy type instance.</span></span>

<span data-ttu-id="cd0de-266">Další informace najdete v tématu [práce se proxy](https://blogs.msdn.com/b/adonet/archive/2011/02/02/using-dbcontext-in-ef-feature-ctp5-part-8-working-with-proxies.aspx) na blogu Entity Framework týmu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-266">For more information, see [Working with Proxies](https://blogs.msdn.com/b/adonet/archive/2011/02/02/using-dbcontext-in-ef-feature-ctp5-part-8-working-with-proxies.aspx) on the Entity Framework team blog.</span></span>

## <a name="disabling-automatic-detection-of-changes"></a><span data-ttu-id="cd0de-267">Zákaz automatického zjišťování změn</span><span class="sxs-lookup"><span data-stu-id="cd0de-267">Disabling Automatic Detection of Changes</span></span>

<span data-ttu-id="cd0de-268">Entity Framework určuje, jak se entita změnila (takže se aktualizace musí odeslat do databáze) porovnáním aktuálních hodnot entity s původními hodnotami.</span><span class="sxs-lookup"><span data-stu-id="cd0de-268">The Entity Framework determines how an entity has changed (and therefore which updates need to be sent to the database) by comparing the current values of an entity with the original values.</span></span> <span data-ttu-id="cd0de-269">Původní hodnoty se uloží při dotazování nebo připojení entity.</span><span class="sxs-lookup"><span data-stu-id="cd0de-269">The original values are stored when the entity was queried or attached.</span></span> <span data-ttu-id="cd0de-270">Některé z metod, které způsobují automatickou detekci změn, jsou následující:</span><span class="sxs-lookup"><span data-stu-id="cd0de-270">Some of the methods that cause automatic change detection are the following:</span></span>

- `DbSet.Find`
- `DbSet.Local`
- `DbSet.Remove`
- `DbSet.Add`
- `DbSet.Attach`
- `DbContext.SaveChanges`
- `DbContext.GetValidationErrors`
- `DbContext.Entry`
- `DbChangeTracker.Entries`

<span data-ttu-id="cd0de-271">Pokud sledujete velký počet entit a v rámci smyčky několikrát voláte jednu z těchto metod, můžete dosáhnout výrazného zlepšení výkonu tím, že se pomocí vlastnosti [AutoDetectChangesEnabled](https://msdn.microsoft.com/library/system.data.entity.infrastructure.dbcontextconfiguration.autodetectchangesenabled(VS.103).aspx) dočasně vypne funkce automatického zjišťování změn.</span><span class="sxs-lookup"><span data-stu-id="cd0de-271">If you're tracking a large number of entities and you call one of these methods many times in a loop, you might get significant performance improvements by temporarily turning off automatic change detection using the [AutoDetectChangesEnabled](https://msdn.microsoft.com/library/system.data.entity.infrastructure.dbcontextconfiguration.autodetectchangesenabled(VS.103).aspx) property.</span></span> <span data-ttu-id="cd0de-272">Další informace najdete v tématu [automatické zjišťování změn](https://blogs.msdn.com/b/adonet/archive/2011/02/06/using-dbcontext-in-ef-feature-ctp5-part-12-automatically-detecting-changes.aspx).</span><span class="sxs-lookup"><span data-stu-id="cd0de-272">For more information, see [Automatically Detecting Changes](https://blogs.msdn.com/b/adonet/archive/2011/02/06/using-dbcontext-in-ef-feature-ctp5-part-12-automatically-detecting-changes.aspx).</span></span>

## <a name="disabling-validation-when-saving-changes"></a><span data-ttu-id="cd0de-273">Zakázání ověřování při ukládání změn</span><span class="sxs-lookup"><span data-stu-id="cd0de-273">Disabling Validation When Saving Changes</span></span>

<span data-ttu-id="cd0de-274">Když zavoláte `SaveChanges` metodu, ve výchozím nastavení Entity Framework před aktualizací databáze ověřuje data ve všech vlastnostech všech změněných entit.</span><span class="sxs-lookup"><span data-stu-id="cd0de-274">When you call the `SaveChanges` method, by default the Entity Framework validates the data in all properties of all changed entities before updating the database.</span></span> <span data-ttu-id="cd0de-275">Pokud jste aktualizovali velký počet entit a již jste ověřili data, je tato práce nepotřebná a je možné, že proces uložení změn trvá méně času tím, že se dočasně vypne ověřování.</span><span class="sxs-lookup"><span data-stu-id="cd0de-275">If you've updated a large number of entities and you've already validated the data, this work is unnecessary and you could make the process of saving the changes take less time by temporarily turning off validation.</span></span> <span data-ttu-id="cd0de-276">Můžete to udělat pomocí vlastnosti [ValidateOnSaveEnabled](https://msdn.microsoft.com/library/system.data.entity.infrastructure.dbcontextconfiguration.validateonsaveenabled(VS.103).aspx) .</span><span class="sxs-lookup"><span data-stu-id="cd0de-276">You can do that using the [ValidateOnSaveEnabled](https://msdn.microsoft.com/library/system.data.entity.infrastructure.dbcontextconfiguration.validateonsaveenabled(VS.103).aspx) property.</span></span> <span data-ttu-id="cd0de-277">Další informace najdete v tématu [ověření](https://blogs.msdn.com/b/adonet/archive/2010/12/15/ef-feature-ctp5-validation.aspx).</span><span class="sxs-lookup"><span data-stu-id="cd0de-277">For more information, see [Validation](https://blogs.msdn.com/b/adonet/archive/2010/12/15/ef-feature-ctp5-validation.aspx).</span></span>

## <a name="summary"></a><span data-ttu-id="cd0de-278">Shrnutí</span><span class="sxs-lookup"><span data-stu-id="cd0de-278">Summary</span></span>

<span data-ttu-id="cd0de-279">Tím se dokončí Tato série kurzů na používání Entity Framework v aplikaci ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="cd0de-279">This completes this series of tutorials on using the Entity Framework in an ASP.NET MVC application.</span></span> <span data-ttu-id="cd0de-280">Odkazy na další prostředky Entity Framework najdete v [mapě obsahu pro přístup k datům ASP.NET](../../../../whitepapers/aspnet-data-access-content-map.md).</span><span class="sxs-lookup"><span data-stu-id="cd0de-280">Links to other Entity Framework resources can be found in the [ASP.NET Data Access Content Map](../../../../whitepapers/aspnet-data-access-content-map.md).</span></span>

<span data-ttu-id="cd0de-281">Další informace o tom, jak nasadit webovou aplikaci po sestavení, naleznete v tématu [Mapa obsahu nasazení ASP.NET](https://msdn.microsoft.com/library/bb386521.aspx) v knihovně MSDN.</span><span class="sxs-lookup"><span data-stu-id="cd0de-281">For more information about how to deploy your web application after you've built it, see [ASP.NET Deployment Content Map](https://msdn.microsoft.com/library/bb386521.aspx) in the MSDN Library.</span></span>

<span data-ttu-id="cd0de-282">Informace o dalších tématech souvisejících s MVC, jako je ověřování a autorizace, najdete v tématu [Doporučené prostředky MVC](../../getting-started/recommended-resources-for-mvc.md).</span><span class="sxs-lookup"><span data-stu-id="cd0de-282">For information about other topics related to MVC, such as authentication and authorization, see the [MVC Recommended Resources](../../getting-started/recommended-resources-for-mvc.md).</span></span>

<a id="acknowledgments"></a>

## <a name="acknowledgments"></a><span data-ttu-id="cd0de-283">Poděkování</span><span class="sxs-lookup"><span data-stu-id="cd0de-283">Acknowledgments</span></span>

- <span data-ttu-id="cd0de-284">Dykstra napsal původní verzi tohoto kurzu a je vedoucím programovacím autorem týmu obsahu pro webové platformy a nástroje Microsoftu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-284">Tom Dykstra wrote the original version of this tutorial and is a senior programming writer on the Microsoft Web Platform and Tools Content Team.</span></span>
- <span data-ttu-id="cd0de-285">[Rick Anderson](https://blogs.msdn.com/b/rickandy/) (Twitter [@RickAndMSFT](http://twitter.com/RickAndMSFT) ) spoluvytvořil tento kurz a většinu práce aktualizovala pro EF 5 a MVC 4.</span><span class="sxs-lookup"><span data-stu-id="cd0de-285">[Rick Anderson](https://blogs.msdn.com/b/rickandy/) (twitter [@RickAndMSFT](http://twitter.com/RickAndMSFT)) co-authored this tutorial and did most of the work updating it for EF 5 and MVC 4.</span></span> <span data-ttu-id="cd0de-286">Rick je hlavní programovací zapisovač pro zaměření Microsoftu na Azure a MVC.</span><span class="sxs-lookup"><span data-stu-id="cd0de-286">Rick is a senior programming writer for Microsoft focusing on Azure and MVC.</span></span>
- <span data-ttu-id="cd0de-287">[Rowan Miller](http://www.romiller.com) a další členové týmu Entity Framework pomáhat s revizemi kódu a pomohli ladit mnoho problémů s migracemi, které vznikly během aktualizace kurzu pro EF 5.</span><span class="sxs-lookup"><span data-stu-id="cd0de-287">[Rowan Miller](http://www.romiller.com) and other members of the Entity Framework team assisted with code reviews and helped debug many issues with migrations that arose while we were updating the tutorial for EF 5.</span></span>

## <a name="vb"></a><span data-ttu-id="cd0de-288">VB</span><span class="sxs-lookup"><span data-stu-id="cd0de-288">VB</span></span>

<span data-ttu-id="cd0de-289">V době, kdy byl kurz původně vytvořen, jsme poskytovali verzi dokončeného projektu stažení v jazyce C# i VB.</span><span class="sxs-lookup"><span data-stu-id="cd0de-289">When the tutorial was originally produced, we provided both C# and VB versions of the completed download project.</span></span> <span data-ttu-id="cd0de-290">V této aktualizaci poskytujeme projekt ke stažení v jazyce C# pro každou kapitolu, aby bylo snazší začít kdekoli v řadě, ale kvůli časovým omezením a dalším prioritám, které jsme v jazyce VB nevytvořili.</span><span class="sxs-lookup"><span data-stu-id="cd0de-290">With this update we are providing a C# downloadable project for each chapter to make it easier to get started anywhere in the series, but due to time limitations and other priorities we did not do that for VB.</span></span> <span data-ttu-id="cd0de-291">Pokud sestavíte projekt VB pomocí těchto kurzů a chcete ho sdílet s ostatními, dejte nám prosím informace.</span><span class="sxs-lookup"><span data-stu-id="cd0de-291">If you build a VB project using these tutorials and would be willing to share that with others, please let us know.</span></span>

<a id="errors"></a>

## <a name="errors-and-workarounds"></a><span data-ttu-id="cd0de-292">Chyby a alternativní řešení</span><span class="sxs-lookup"><span data-stu-id="cd0de-292">Errors and Workarounds</span></span>

### <a name="cannot-createshadow-copy"></a><span data-ttu-id="cd0de-293">Nelze vytvořit/stínovou kopii</span><span class="sxs-lookup"><span data-stu-id="cd0de-293">Cannot create/shadow copy</span></span>

<span data-ttu-id="cd0de-294">Chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-294">Error Message:</span></span>

<span data-ttu-id="cd0de-295">*Nelze vytvořit nebo stínovou kopii DotNetOpenAuth. OpenId, pokud tento soubor již existuje.*</span><span class="sxs-lookup"><span data-stu-id="cd0de-295">*Cannot create/shadow copy 'DotNetOpenAuth.OpenId' when that file already exists.*</span></span>

<span data-ttu-id="cd0de-296">Řešení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-296">Solution:</span></span>

<span data-ttu-id="cd0de-297">Počkejte několik sekund a aktualizujte stránku.</span><span class="sxs-lookup"><span data-stu-id="cd0de-297">Wait a few seconds and refresh the page.</span></span>

### <a name="update-database-not-recognized"></a><span data-ttu-id="cd0de-298">Aktualizace – databáze nebyla rozpoznána.</span><span class="sxs-lookup"><span data-stu-id="cd0de-298">Update-Database not recognized</span></span>

<span data-ttu-id="cd0de-299">Chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-299">Error Message:</span></span>

<span data-ttu-id="cd0de-300">*Termín "aktualizace databáze" nebyl rozpoznán jako název rutiny, funkce, souboru skriptu nebo spustitelného programu. Zkontrolujte pravopis názvu, nebo pokud byla vložena cesta, ověřte, zda je cesta správná, a akci opakujte.* (Z *`Update-Database`* příkazu v PMC.)</span><span class="sxs-lookup"><span data-stu-id="cd0de-300">*The term 'Update-Database' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the spelling of the name, or if a path was included, verify that the path is correct and try again.*(From the *`Update-Database`* command in the PMC.)</span></span>

<span data-ttu-id="cd0de-301">Řešení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-301">Solution:</span></span>

<span data-ttu-id="cd0de-302">Ukončete aplikaci Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="cd0de-302">Exit Visual Studio.</span></span> <span data-ttu-id="cd0de-303">Znovu otevřete projekt a zkuste to znovu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-303">Reopen project and try again.</span></span>

### <a name="validation-failed"></a><span data-ttu-id="cd0de-304">Neúspěšné ověření</span><span class="sxs-lookup"><span data-stu-id="cd0de-304">Validation failed</span></span>

<span data-ttu-id="cd0de-305">Chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-305">Error Message:</span></span>

<span data-ttu-id="cd0de-306">*Nepodařilo se ověřit jednu nebo více entit. Další podrobnosti najdete ve vlastnosti ' EntityValidationErrors '.*</span><span class="sxs-lookup"><span data-stu-id="cd0de-306">*Validation failed for one or more entities. See 'EntityValidationErrors' property for more details.*</span></span> <span data-ttu-id="cd0de-307">(Z *`Update-Database`* příkazu v PMC.)</span><span class="sxs-lookup"><span data-stu-id="cd0de-307">(From the *`Update-Database`* command in the PMC.)</span></span>

<span data-ttu-id="cd0de-308">Řešení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-308">Solution:</span></span>

<span data-ttu-id="cd0de-309">Jednou z příčin tohoto problému jsou chyby ověření při `Seed` spuštění metody.</span><span class="sxs-lookup"><span data-stu-id="cd0de-309">One cause of this problem is validation errors when the `Seed` method runs.</span></span> <span data-ttu-id="cd0de-310">Tipy k ladění metody naleznete v tématu [osazení a ladění Entity Framework (EF) databáze](https://blogs.msdn.com/b/rickandy/archive/2013/02/12/seeding-and-debugging-entity-framework-ef-dbs.aspx) `Seed` .</span><span class="sxs-lookup"><span data-stu-id="cd0de-310">See [Seeding and Debugging Entity Framework (EF) DBs](https://blogs.msdn.com/b/rickandy/archive/2013/02/12/seeding-and-debugging-entity-framework-ef-dbs.aspx) for tips on debugging the `Seed` method.</span></span>

### <a name="http-50019-error"></a><span data-ttu-id="cd0de-311">Chyba HTTP 500,19</span><span class="sxs-lookup"><span data-stu-id="cd0de-311">HTTP 500.19 error</span></span>

<span data-ttu-id="cd0de-312">Chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-312">Error Message:</span></span>

<span data-ttu-id="cd0de-313">*Chyba protokolu HTTP 500,19 – vnitřní chyba serveru: k požadované stránce nelze přistupovat, protože související konfigurační data stránky jsou neplatná.*</span><span class="sxs-lookup"><span data-stu-id="cd0de-313">*HTTP Error 500.19 - Internal Server Error The requested page cannot be accessed because the related configuration data for the page is invalid.*</span></span>

<span data-ttu-id="cd0de-314">Řešení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-314">Solution:</span></span>

<span data-ttu-id="cd0de-315">Jedním ze způsobů, jak získat tuto chybu, je použití více kopií řešení, přičemž každý z nich používá stejné číslo portu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-315">One way you can get this error is from having multiple copies of the solution, each of them using the same port number.</span></span> <span data-ttu-id="cd0de-316">Tento problém obvykle můžete vyřešit ukončením všech instancí aplikace Visual Studio a následným restartováním projektu, na kterém pracujete.</span><span class="sxs-lookup"><span data-stu-id="cd0de-316">You can usually solve this problem by exiting all instances of Visual Studio, then restarting the project your working on.</span></span> <span data-ttu-id="cd0de-317">Pokud to nepomůže, zkuste změnit číslo portu.</span><span class="sxs-lookup"><span data-stu-id="cd0de-317">If that doesn't work, try changing the port number.</span></span> <span data-ttu-id="cd0de-318">Klikněte pravým tlačítkem na soubor projektu a pak klikněte na vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="cd0de-318">Right click on the project file and then click properties.</span></span> <span data-ttu-id="cd0de-319">Vyberte kartu **Web** a potom změňte číslo portu v textovém poli **Adresa URL projektu** .</span><span class="sxs-lookup"><span data-stu-id="cd0de-319">Select the **Web** tab and then change the port number in the **Project Url** text box.</span></span>

### <a name="error-locating-sql-server-instance"></a><span data-ttu-id="cd0de-320">Při hledání instance SQL Server došlo k chybě.</span><span class="sxs-lookup"><span data-stu-id="cd0de-320">Error locating SQL Server instance</span></span>

<span data-ttu-id="cd0de-321">Chybová zpráva:</span><span class="sxs-lookup"><span data-stu-id="cd0de-321">Error Message:</span></span>

<span data-ttu-id="cd0de-322">*Při navazování připojení k SQL Server došlo k chybě související se sítí nebo instanci. Server nebyl nalezen nebo k němu nelze získat přístup. Ověřte, zda je název instance správný a zda je SQL Server nakonfigurovaná tak, aby povolovala vzdálená připojení. (poskytovatel: síťová rozhraní SQL, chyba: 26 – Chyba při hledání zadaného serveru nebo instance)*</span><span class="sxs-lookup"><span data-stu-id="cd0de-322">*A network-related or instance-specific error occurred while establishing a connection to SQL Server. The server was not found or was not accessible. Verify that the instance name is correct and that SQL Server is configured to allow remote connections. (provider: SQL Network Interfaces, error: 26 - Error Locating Server/Instance Specified)*</span></span>

<span data-ttu-id="cd0de-323">Řešení:</span><span class="sxs-lookup"><span data-stu-id="cd0de-323">Solution:</span></span>

<span data-ttu-id="cd0de-324">Ověřte připojovací řetězec.</span><span class="sxs-lookup"><span data-stu-id="cd0de-324">Check connection string.</span></span> <span data-ttu-id="cd0de-325">Pokud jste databázi odstranili ručně, změňte název databáze v řetězci konstrukce.</span><span class="sxs-lookup"><span data-stu-id="cd0de-325">If you have manually deleted the database, change the name of the database in the construction string.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="cd0de-326">[Předchozí](implementing-the-repository-and-unit-of-work-patterns-in-an-asp-net-mvc-application.md) 
>  [Další](building-the-ef5-mvc4-chapter-downloads.md)</span><span class="sxs-lookup"><span data-stu-id="cd0de-326">[Previous](implementing-the-repository-and-unit-of-work-patterns-in-an-asp-net-mvc-application.md)
[Next](building-the-ef5-mvc4-chapter-downloads.md)</span></span>
